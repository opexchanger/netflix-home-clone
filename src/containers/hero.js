import { Hero } from '../components';
import { Navbar } from '../components';

const Hero = ({children}) => (
    <Hero>
        <Navbar>
            
        </Navbar>
    </Hero>
)

export default Hero;